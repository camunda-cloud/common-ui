import { Meta, Canvas, Description } from '@storybook/addon-docs'
import { CmDropdown, CmContext } from '@camunda-cloud/common-ui-react'

import Readme from './readme.md'

<Meta title="Components/Interactive/Dropdown" component={CmDropdown} />

# Dropdown

Note: A cm-context is required to handle the color for all cm-icons, and cm-dropdown is using a cm-icon,
requiring a cm-context on the page to render correctly.

<Canvas>
	<div
		style={{
			display: 'flex',
			alignItems: 'center',
			justifyContent: 'space-evenly',
		}}
	>
		<CmContext />
		<CmDropdown
			trigger={{
				type: 'button',
				appearance: 'primary',
				label: 'Button Trigger',
			}}
			options={[
				{
					options: [
						{
							label: 'Normal Option',
							value: 'Normal',
							handler: () => {},
						},
						{
							label: 'Dangerous Option',
							value: 'Danger',
							isDangerous: true,
							handler: () => {},
						},
						{
							label: 'Disabled Option',
							value: 'Disabled',
							isDisabled: true,
						},
					],
				},
			]}
		/>
		<CmDropdown
			trigger={{
				type: 'label',
				label: 'Label Trigger',
			}}
			options={[
				{
					options: [
						{
							label: 'Normal Option',
							value: 'Normal',
							handler: () => {},
						},
						{
							label: 'Dangerous Option',
							value: 'Danger',
							isDangerous: true,
							handler: () => {},
						},
						{
							label: 'Disabled Option',
							value: 'Disabled',
							isDisabled: true,
						},
					],
				},
			]}
		/>
		<CmDropdown
			trigger={{
				type: 'icon',
				icon: 'contextMenu',
				label: 'Icon Trigger',
			}}
			options={[
				{
					options: [
						{
							label: 'Normal Option',
							value: 'Normal',
							handler: () => {},
						},
						{
							label: 'Dangerous Option',
							value: 'Danger',
							isDangerous: true,
							handler: () => {},
						},
						{
							label: 'Disabled Option',
							value: 'Disabled',
							isDisabled: true,
						},
					],
				},
			]}
		/>
	</div>
</Canvas>

## Technical Documentation

<Description>{Readme}</Description>
